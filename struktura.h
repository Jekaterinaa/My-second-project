#pragma once
#include "biblioteka1.h"

struct duomuo {
    string vard, pav;
    vector<float> paz;
    int num = 0, nd, egz;
    float vid = 0, gp, med;
};

struct skaicius {
    int kiek;
    int kiek_paz;
};